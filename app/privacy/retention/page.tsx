import type { Metadata } from "next";
import { LegalPage, draftRobots } from "../../legalPage.tsx";
import { createPublicAppUrl } from "../../publicAppOrigin.ts";
import { retentionSchedule } from "../../retentionSchedule.ts";
export const metadata:Metadata={title:"Data Retention Schedule | What’s Your Bride Price?",description:"Implemented and inactive data-retention boundaries for What’s Your Bride Price?.",alternates:{canonical:createPublicAppUrl("/privacy/retention")},robots:draftRobots};
export default function RetentionPage(){return <LegalPage kicker="Technical lifecycle record" title="Data-retention schedule"><p>Implemented periods are distinguished from inactive proposals. A completed result’s authoritative trigger is quiz completion and its maximum active period is exactly 90 days. Publishing has no effect on that expiry timestamp.</p><div className="legal-table-wrap"><table><thead><tr><th>Category</th><th>Purpose and location</th><th>Trigger and maximum</th><th>Expiry and deletion</th><th>Control and state</th></tr></thead><tbody>{retentionSchedule.map((item)=><tr key={item.category}><th scope="row">{item.category}</th><td>{item.purpose}<br/><small>{item.location}</small></td><td>{item.trigger}<br/><strong>{item.maximum}</strong></td><td>{item.expiry}<br/><small>{item.deletion}</small></td><td>{item.control}<br/><strong>{item.active}</strong></td></tr>)}</tbody></table></div><p>Public copies, social previews and files already downloaded or shared outside the service are not controlled by application retention operations.</p></LegalPage>}
